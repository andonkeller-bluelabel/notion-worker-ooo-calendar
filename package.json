{
  "name": "notion-worker-ooo-calendar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Notion Worker that reconciles approved Team OOO Calendar requests onto the BlueLabel Microsoft 365 group calendar via Graph.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts",
    "dev": "ntn workers dev",
    "deploy": "ntn workers deploy"
  },
  "dependencies": {
    "@notionhq/client": "^5.22.0",
    "@notionhq/workers": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.0",
    "typescript": "^5.6.0"
  }
}
